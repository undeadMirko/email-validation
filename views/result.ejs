<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resultados</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1 class="mt-5">Resultados de Validación</h1>

        <h2 class="mt-4">Aprobados</h2>
        <% if (results.approved && results.approved.length > 0) { %>
            <ul class="list-group">
                <% results.approved.forEach(email => { %>
                    <li class="list-group-item"><%= email %></li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No hay correos aprobados.</p>
        <% } %>

        <h2 class="mt-4">No Aprobados</h2>
        <% if (results.notApproved && results.notApproved.length > 0) { %>
            <ul class="list-group">
                <% results.notApproved.forEach(email => { %>
                    <li class="list-group-item"><%= email %></li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No hay correos no aprobados.</p>
        <% } %>

        <h2 class="mt-4">Correos Rebotados</h2>
        <% if (results.bouncing && results.bouncing.length > 0) { %>
            <ul class="list-group">
                <% results.bouncing.forEach(email => { %>
                    <li class="list-group-item"><%= email %></li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No hay correos rebotados.</p>
        <% } %>

        <div class="mt-5">
            <button class="btn btn-primary me-2" onclick="window.location.href='/email/read-bounced';">
                Validar Correos Rebotados
            </button>
            <button class="btn btn-success" onclick="window.location.href='/email/export';">
                Exportar Resultados
            </button>
        </div>
    </div>

    <!-- Verificar el estado de la sesión en results.ejs -->
<p>Resultados de la sesión:</p>
<pre><%= JSON.stringify(results, null, 2) %></pre>
</body>
</html>
